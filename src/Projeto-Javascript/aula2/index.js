console.log('Olá mundo');
var oláMundo = "Olá Mundo";

/**
 * devemos usar var para declarar variáveis em um local específico 
 * assim como let. Atualmente 
 * uds-se let em vez de var;
 * para declarar variáveis constantes como por exemplo a conexão de
 * um servidor ou até mesmo declarar um dado que
 * será usado na aplicação toda, usamos const
 */